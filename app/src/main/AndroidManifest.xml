<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".TradeFarmApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tradefarm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tradefarmplustext"
        android:supportsRtl="true"
        android:theme="@style/Theme.FarmTrade"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity"
            android:label="Masuk"/>
        <activity android:name=".ui.signup.SignUpActivity"
            android:label="Daftar"/>
        <activity android:name=".ui.detailproduct.ActivityDetailProduct"
            android:label="Detail Produk"/>
    </application>

</manifest>